/**
 * Re-export the shared LocationUploadService.
 * Mobile-specific upload extensions can be added here if needed.
 */
export { LocationUploadService } from '../../../shared/gps/LocationUploadService';
