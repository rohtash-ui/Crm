# Copy this file to .env and fill in your key to enable Claude AI scoring/summaries.
# Without a key the app runs on a transparent heuristic — it never hard-fails.
ANTHROPIC_API_KEY=sk-ant-...
PORT=3000
